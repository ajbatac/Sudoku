{
  "name": "create-user-app-monorepo",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "wrangler": "^3.0.0"
  },
  "scripts": {
    "dev": "bun --filter=web dev",
    "build": "bun --filter=web build",
    "deploy": "bun run build && npx wrangler pages deploy apps/web/dist"
  }
}
